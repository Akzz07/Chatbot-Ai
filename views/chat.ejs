<!DOCTYPE html>
<html>
  <head>
    <title>AI Chatbot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body class="bg-light">
    <div class="container mt-5">
      <div class="card shadow">
        <div class="card-header bg-success text-white">
          <h4>AI Chatbot</h4>
        </div>
        <div class="card-body">
          <form method="POST" action="/chat" class="mb-3">
            <input type="text" name="message" class="form-control" placeholder="Tanya sesuatu..." required />
            <button class="btn btn-success mt-2">Kirim</button>
          </form>

          <% if(question){ %>
          <p><strong>Kamu:</strong> <%= question %></p>
          <p><strong>AI:</strong> <%= reply %></p>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
